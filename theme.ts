import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    background: {
      default: "#233", // set your desired default background color
      paper: "#ffffff",
      // set your desired paper/background color for components like cards, dialogs, etc.
    },
  },
});
