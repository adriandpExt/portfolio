import React from "react";
import { Header } from "../header";
import Particle from "../particles/Particle";

export const Home = () => {
  return (
    <React.Fragment>
      <Header />
      <Particle />
    </React.Fragment>
  );
};

export default Home;
