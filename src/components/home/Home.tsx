import React from "react";
import { Header } from "../header";
import { Particles } from "../particles";

export const Home = () => {
  return (
    <React.Fragment>
      <Header />
      <Particles />
    </React.Fragment>
  );
};

export default Home;
